<p id="notice"><%= notice %></p>

<h1>Listing Authenticates Temps</h1>

<table>
  <thead>
    <tr>
      <th>Params</th>
      <th>Status</th>
      <th>Guid</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @authenticates_temps.each do |authenticates_temp| %>
      <tr>
        <td><%= authenticates_temp.params %></td>
        <td><%= authenticates_temp.status %></td>
        <td><%= authenticates_temp.guid %></td>
        <td><%= link_to 'Show', authenticates_temp %></td>
        <td><%= link_to 'Edit', edit_authenticates_temp_path(authenticates_temp) %></td>
        <td><%= link_to 'Destroy', authenticates_temp, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Authenticates temp', new_authenticates_temp_path %>
